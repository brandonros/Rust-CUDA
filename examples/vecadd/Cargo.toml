[package]
name = "vecadd"
version = "0.1.0"
edition = "2024"

[features]
default = []
# Build the kernels crate with the LLVM 21 backend. Forwards to `cuda_builder`,
# which propagates the feature to `nvvm` (default arch -> Compute100 / Blackwell)
# and to its nested `cargo build -p rustc_codegen_nvvm`.
llvm21 = ["cuda_builder/llvm21"]

[dependencies]
cust = { path = "../../crates/cust" }
cust_raw = { path = "../../crates/cust_raw", default-features = false, features = ["driver"] }
nanorand = "0.7"

[build-dependencies]
cuda_builder = { workspace = true, default-features = false }
